import React from 'react'

export default function TeammateForm(prop) {

    const { values, onSubmit } = props 


    return (
    <form>
        <div>
            <h2>Join the Team!</h2>
            <button>Submit</button>
            </div>     
    </form>
    )
}